PORT=3001
HOST=0.0.0.0
STREAM_SECRET=change-me-to-a-long-random-string
PYTHON_PATH=python3
DOWNLOAD_DIR=./data/downloads
# 繁中預設根網址；改 https://missav.ws/en 則標題為英文
MISSAV_BASE_URL=https://missav.ws
# 留空或設為 * / true 表示反射請求來源（本機最省事）；正式環境請設具體網域
CORS_ORIGIN=
# 留空時 m3u8 與分片網址使用相對路徑 /api/stream（搭配 Vite proxy 或同源反代）
PUBLIC_BASE_URL=
# 設為 1 時 MissAV 影片頁 HTML 強制只用 Python；預設先 Node 再回退 Python
# MISSAV_HTML_PYTHON_ONLY=
# 下載佇列同時執行的 Python 工作數，預設 6，上限 16
# DOWNLOAD_QUEUE_CONCURRENCY=6
# 縮圖解析時同時抓 MissAV HTML 數，預設 24，上限 48
# THUMB_HTML_FETCH_CONCURRENCY=24
# 詳情／預覽等 fetchVideoPage 全域併發，預設 20，上限 48
# VIDEO_PAGE_FETCH_CONCURRENCY=20
# undici 對同一 origin 併發連線（串流分片、縮圖、HTML），預設 192，上限 384
# UPSTREAM_CONNECTIONS_PER_ORIGIN=192
# THUMB_PARSE_CACHE_TTL_MS=900000
# THUMB_PARSE_CACHE_MAX_ENTRIES=2048
# Recombee：逾時毫秒、429/5xx 重試次數、連線池大小、首頁單次筆數上限
# RECOMBEE_TIMEOUT_MS=25000
# RECOMBEE_RETRIES=3
# RECOMBEE_CONNECTIONS=24
# FEATURED_MAX_LIMIT=100
